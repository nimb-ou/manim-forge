# --- Phase 0: render harness -------------------------------------------------
manim==0.21.0            # Manim Community Edition. NOT manimgl — the two APIs
                         # are incompatible; rows are tagged `flavor` to keep
                         # them apart. See docs/STATE.md.
# system deps (Homebrew): cairo pango pkg-config ffmpeg, plus BasicTeX

# --- Phase 1: ingestion ------------------------------------------------------
datasets>=3.0
huggingface-hub>=0.26
pyarrow>=17.0

# --- Phase 6: evaluation -----------------------------------------------------
scikit-image>=0.24       # SSIM over sampled frames
torch>=2.4               # CLIP backbone
transformers>=4.45       # openai/clip-vit-base-patch32 (already cached locally)

# --- Phase 7: local platform -------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.32

# --- Phase 5: fine-tuning (Apple Silicon) ------------------------------------
# installed separately (not on PyPI for every platform):
#   pip install mlx>=0.21 mlx-lm>=0.20

# --- development -------------------------------------------------------------
pytest>=8.0              # tests/ — run with ./.venv/bin/python -m pytest -q

# --- training on Kaggle ------------------------------------------------------
kaggle>=2.2                # CLI 2.2 replaced username+key auth with a bearer token
